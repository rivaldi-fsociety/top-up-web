export type SliderProps = { id: number; src: string }
export type GamesProps = { id: number; name: string, src: string, category_id: number; is_default: boolean }
export type TabProps = { tabIndex: number; category_id: number;}
export type TopUpProps = { id: number; point: number, harga: string; }